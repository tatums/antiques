<% Category.active.order(:position).each do |category|%>
  <%= content_tag_for :li, category do %>
	  <%= link_to upcase_title(category), category %> 
	    <% if user_signed_in? %>
	      <%= activate_deactivate_category(category) %>
	    <% end %>
	<% end %>
<% end %>
