<%= form_for @user, :html =>{:class=> 'nice'} do |f| %>


<% if @user.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@user.errors.count, "error") %> prohibited this image from being saved:</h2>

    <ul>
    <% @user.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>

  <div class="field">
    <%= f.password_field :password, :class=> "input-text", :placeholder => 'New Password'  %>
  </div>
  <div class="field">
    <%= f.password_field :password_confirmation, :class=> "input-text", :placeholder => 'Password Confirmation'  %>
  </div>
  
  <div class="actions">
    <%= f.submit 'Save', :class => "nice small radius button" %>
  </div>
  

<% end %>