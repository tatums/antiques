/* Add the new keyword to the bottom of the keyword list */
$("#keywords").append("<%= escape_javascript(render(@keyword)) %>");

/* Highlight the new comment */
$('#keyword_<%= @keyword.id %>').effect("highlight", {}, 3000);

$("#keyword_title").val('');
$('#keyword-submit').attr('disabled','disabled');
