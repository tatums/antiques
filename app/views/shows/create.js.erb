/* Add the new keyword to the bottom of the keyword list */
$("#shows").append("<%= escape_javascript(render(@show)) %>");

/* Highlight the new comment */
$('#show_<%= @show.id %>').effect("highlight", {}, 3000);

$('#myModal .close-reveal-modal').trigger('click.modalEvent');
