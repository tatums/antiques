= content_tag :div, note, id: "note_#{note.id}", class: 'well' do
  %span.label.label-important.delete-note
    = link_to 'Delete', note, method: 'delete', remote:true
  %p.notes-p
    %i
      = note.created_at.strftime("%B %d, %Y at %I:%M %P")
  %p.notes-p
    = note.body    
